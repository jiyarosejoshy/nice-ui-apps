apiVersion: v1
kind: Service
metadata:
  name: project-c-service
spec:
  type: NodePort
  selector:
    app: project-c
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30013

